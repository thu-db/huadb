ci:
  script:
    - git clone https://gitlab-ci-token:${TEST_REPO_TOKEN}@git.tsinghua.edu.cn/dbtrain/huadb-test.git
    - cp -r src third_party *.txt huadb-test
    - cd huadb-test
    - bash test.sh
